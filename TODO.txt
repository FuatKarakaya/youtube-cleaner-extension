remove * from youtube link
nothing works when the icon is clicked
the script only works once


run for loaded videos:
    if the video is in screen:
        cleaner_control()
    else:
        addEventListener(OnVisible, cleaner_control)

addEventListener to check if new videos are coming
